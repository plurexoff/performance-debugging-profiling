# 🔍 Задание №8: Отладка и профилирование производительности

Комплексное руководство по поиску, анализу и оптимизации проблем производительности в Python коде.

## 📋 Цель задания

Научиться находить и устранять проблемы производительности и памяти с помощью инструментов профилирования.

## 🎯 Структура задания

### 1️⃣ **Шаг 1: Профилирование (Поиск узких мест)**
- Использование `cProfile` - встроенный профилировщик
- Использование `line_profiler` - построчное профилирование
- Использование `memory_profiler` - профилирование памяти
- Использование `py-spy` - визуализация пламени

### 2️⃣ **Шаг 2: Анализ результатов**
- Интерпретация отчетов профилирования
- Выявление «горячих точек» (bottlenecks)
- Определение приоритетов оптимизации

### 3️⃣ **Шаг 3: Оптимизация**
- Рефакторинг проблемных участков
- Применение оптимальных алгоритмов
- Повторное измерение производительности

## 📁 Структура проекта

```
performance-debugging-profiling/
├── README.md                    # Этот файл
├── requirements.txt             # Зависимости проекта
├── src/
│   ├── __init__.py
│   ├── slow_algorithms.py       # Медленные алгоритмы с проблемами
│   ├── optimized_algorithms.py  # Оптимизированные версии
│   ├── memory_issues.py         # Код с проблемами памяти
│   └── utils.py                 # Вспомогательные функции
├── profiling/
│   ├── profile_cpu.py           # CPU профилирование (cProfile)
│   ├── profile_memory.py        # Профилирование памяти
│   ├── profile_line.py          # Построчное профилирование
│   └── benchmark.py             # Сравнительные тесты
├── reports/
│   ├── initial_analysis.md      # Начальный анализ проблем
│   ├── optimization_report.md   # Отчет об оптимизации
│   └── profiling_results/       # Сохраненные профили (.prof, .svg)
└── scripts/
    ├── run_profiling.sh         # Скрипт для запуска профилирования
    └── compare_performance.py   # Сравнение до/после оптимизации
```

## 🚀 Быстрый старт

### Установка зависимостей
```bash
# Клонирование репозитория
git clone https://github.com/plurexoff/performance-debugging-profiling.git
cd performance-debugging-profiling

# Создание виртуального окружения
python -m venv venv

# Активация окружения
# Linux/Mac:
source venv/bin/activate
# Windows:
venv\Scripts\activate

# Установка зависимостей
pip install -r requirements.txt
```

### Запуск примеров

#### 1. Базовое профилирование CPU (cProfile)
```bash
python profiling/profile_cpu.py
```

#### 2. Профилирование памяти
```bash
python profiling/profile_memory.py
```

#### 3. Построчное профилирование
```bash
python profiling/profile_line.py
```

#### 4. Визуализация профилирования (flamegraph)
```bash
pip install py-spy
py-spy record -o profile.svg -- python src/slow_algorithms.py
```

## 📊 Инструменты профилирования

### 1. **cProfile** - CPU профилирование
Встроенный модуль Python для анализа времени выполнения.

**Использование:**
```python
import cProfile
import pstats

# Запуск с профилированием
cProfile.run('your_function()', 'output.prof')

# Анализ результатов
stats = pstats.Stats('output.prof')
stats.sort_stats('cumulative').print_stats(10)
```

**Ключевые метрики:**
- `ncalls` - количество вызовов функции
- `tottime` - общее время в функции (без вложений)
- `cumtime` - время с учетом вложенных вызовов
- `percall` - среднее время одного вызова

### 2. **line_profiler** - Построчное профилирование
Позволяет видеть, какие строки кода замедляют выполнение.

**Использование:**
```bash
pip install line_profiler

# Добавить декоратор @profile к функции
python -m kernprof -l -v script.py
```

### 3. **memory_profiler** - Анализ использования памяти
Отслеживает потребление памяти по строкам кода.

**Использование:**
```bash
pip install memory_profiler psutil

# Добавить декоратор @profile к функции
python -m memory_profiler script.py
```

**Результат:**
```
Line #    Mem usage  Increment  Line Contents
================================================
    10    39.5 MiB   39.5 MiB   @profile
    11    39.5 MiB    0.0 MiB   def your_function():
    12    42.3 MiB    2.8 MiB       data = [i for i in range(100000)]
```

### 4. **py-spy** - Визуализация профилирования
Генерирует flame graphs без изменения кода.

**Использование:**
```bash
pip install py-spy

# Запись профиля
py-spy record -o profile.svg -- python your_script.py

# Просмотр в реальном времени
py-spy top -- python your_script.py
```

## 📈 Примеры проблем и решений

### ❌ Проблема 1: Неэффективный поиск в списке
```python
# МЕДЛЕННО: O(n) на каждое обращение
def slow_search(data, item):
    for x in data:
        if x == item:
            return True
    return False

# БЫСТРО: O(1) в среднем
def fast_search(data, item):
    return item in set(data)
```

### ❌ Проблема 2: Повторяющиеся вычисления
```python
# МЕДЛЕННО: вычисляет одно и то же много раз
def slow_fibonacci(n):
    if n <= 1:
        return n
    return slow_fibonacci(n-1) + slow_fibonacci(n-2)

# БЫСТРО: кэширование результатов
from functools import lru_cache

@lru_cache(maxsize=None)
def fast_fibonacci(n):
    if n <= 1:
        return n
    return fast_fibonacci(n-1) + fast_fibonacci(n-2)
```

### ❌ Проблема 3: Утечка памяти
```python
# ПРОБЛЕМА: объекты остаются в памяти
class DataProcessor:
    cache = {}  # Растет бесконечно!
    
    @classmethod
    def process(cls, data):
        cls.cache[id(data)] = expensive_computation(data)

# РЕШЕНИЕ: используйте лимит кэша
from functools import lru_cache

@lru_cache(maxsize=128)
def process(data):
    return expensive_computation(data)
```

### ❌ Проблема 4: Неэффективная работа с файлами
```python
# МЕДЛЕННО: читает весь файл в память
with open('huge_file.txt') as f:
    all_data = f.read()  # Может не поместиться в памяти!

# БЫСТРО: обработка по частям
with open('huge_file.txt') as f:
    for chunk in iter(lambda: f.read(8192), ''):
        process_chunk(chunk)
```

## 🔧 Практический пример: Полный цикл оптимизации

### Шаг 1: Выявление проблемы
```bash
python profiling/profile_cpu.py
```

Результат показывает, что функция `process_data()` занимает 95% времени.

### Шаг 2: Детальный анализ
```bash
python -m line_profiler profile_line.py
```

Видим, что строка 42 (вложенный цикл) выполняется 1 млн раз.

### Шаг 3: Оптимизация
```python
# Было: O(n²)
result = []
for i in range(10000):
    for j in range(10000):
        if i in forbidden_list:  # Поиск O(n)
            result.append((i, j))

# Стало: O(n log n)
forbidden_set = set(forbidden_list)  # O(n)
result = []
for i in range(10000):
    if i not in forbidden_set:  # Поиск O(1)
        result.extend([(i, j) for j in range(10000)])
```

### Шаг 4: Проверка результатов
```bash
python scripts/compare_performance.py
```

Результат: **50x ускорение** (со 1000ms → 20ms)

## 📋 Метрики оптимизации

Заполните этот шаблон при оптимизации:

| Метрика | До оптимизации | После оптимизации | Улучшение |
|---------|---|---|---|
| Время выполнения | 1000 ms | 50 ms | **20x ⚡** |
| Использование памяти | 500 MB | 50 MB | **10x 💾** |
| CPU usage | 95% | 15% | **6x 🔥** |
| Количество вызовов функции | 10,000,000 | 100,000 | **100x 🎯** |

## 💡 Лучшие практики

### ✅ ДО ОПТИМИЗАЦИИ
1. **Профилируй реальные данные** - используй данные, близкие к production
2. **Измеряй несколько раз** - устраняй случайные колебания
3. **Профилируй весь код** - не угадывай, где узкое место
4. **Используй несколько инструментов** - разные инструменты ловят разные проблемы

### ✅ ПРИНЦИП 80/20
- 80% времени приходится на 20% кода
- Сосредоточься на «горячих точках» (hot spots)
- Не оптимизируй код, который редко выполняется

### ✅ ИЗМЕРЯЙ РЕЗУЛЬТАТЫ
```python
import time
from functools import wraps

def benchmark(func):
    @wraps(func)
    def wrapper(*args, **kwargs):
        start = time.perf_counter()
        result = func(*args, **kwargs)
        end = time.perf_counter()
        print(f"{func.__name__}: {end - start:.4f}s")
        return result
    return wrapper

@benchmark
def my_function():
    # ваш код
    pass
```

### ✅ РЕГУЛЯРНОЕ ПРОФИЛИРОВАНИЕ
- Профилируй после каждого значительного изменения кода
- Веди историю метрик производительности
- Интегрируй профилирование в CI/CD pipeline

## 📚 Дополнительные ресурсы

- [Python cProfile документация](https://docs.python.org/3/library/profile.html)
- [line_profiler GitHub](https://github.com/pyutils/line_profiler)
- [memory_profiler GitHub](https://github.com/pythonprofilers/memory_profiler)
- [py-spy GitHub](https://github.com/benfred/py-spy)
- [Валлис Valgrind для Linux](https://valgrind.org/)

## 📝 Что нужно сдать

1. ✅ Этот репозиторий со всеми примерами
2. ✅ Отчет в `reports/initial_analysis.md` с выявленными проблемами
3. ✅ Отчет в `reports/optimization_report.md` с описанием оптимизаций
4. ✅ Сравнение результатов до/после в виде таблицы метрик
5. ✅ Профили профилирования (`.prof` файлы или снимки экрана)

## 🤝 Автор

Создано для задания по отладке и профилированию производительности.

---

**Happy profiling! 🚀**